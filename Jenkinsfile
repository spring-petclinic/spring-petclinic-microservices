pipeline {
    agent any

    stages {
        stage('PWD') {
            pwd
        }

        stage('Test') {
            steps {
                echo 'Testing ...'
            }
        }

        stage('Build') {
            steps {
                echo 'Building ...'
            }
        }
    }
}