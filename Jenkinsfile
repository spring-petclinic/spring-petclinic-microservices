pipeline {
    agent any

    stages {
        stage('PWD') {
            steps {
                sh 'pwd'
            }
        }

        stage('Test') {
            steps {
                echo 'Testing ...'
            }
        }

        stage('Build') {
            steps {
                echo 'Building ...'
            }
        }
    }
}