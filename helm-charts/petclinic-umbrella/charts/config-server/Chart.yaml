# helm-charts/petclinic-umbrella/charts/config-server/Chart.yaml
apiVersion: v2
name: config-server
description: Helm chart for Spring PetClinic Config Server
# type - The type of the chart. Defaults to 'application'. We keep this.
type: application

# version - This is the chart version. Increment this when changing the chart.
version: 0.1.0

# appVersion - The version of the app being deployed. This can be updated independently.
# For now, let's align it with the chart version or use the app's actual version if known.
appVersion: "3.4.1" # Example, use appropriate app version